model Address {
  id         String  @id @default(cuid())
  fullname   String
  address    String
  region     String?
  nearby     String?
  zipcode    String
  city       String
  state      String
  country    String
  regionName String
  // userId    String
  active     Boolean @default(true)
}
